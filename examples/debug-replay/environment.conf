# Debug replay context — jumphost staging environment
# Used with debug-replay.shbang to reconstruct a prod shard locally for debugging.
#
# All config lives in developerScratchPad.hocon — staging host, archive date,
# shard layout, and breakpoint details. In real use that file is gitignored
# and each developer maintains their own copy.
# When sh.bang supports multiple --ctx files the include goes away entirely.

include "developerScratchPad.hocon"
